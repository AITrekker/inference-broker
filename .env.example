# Optional provider credentials, read by execution grants.
# Never commit a real .env. The MockProvider needs no credentials.

OPENAI_API_KEY=
ANTHROPIC_API_KEY=
HF_TOKEN=

# Optional: override the local sealedx data directory (default: ~/.sealedx)
# SEALEDX_HOME=
